GNU Make 3.81
Copyright (C) 2006  Free Software Foundation, Inc.
该程序为自由软件，详情可参阅版权条款。在法律允许的范围内
我们不作任何担保，这包含但不限于任何商业适售性以及针对特
定目的的适用性的担保。

 这个程序创建为 i686-pc-linux-gnu
正在读入 makefiles...
正在读入 makefile “redis_mk”...
正在读入 makefile “redis_mk-settings” (搜索路径) (没有 ~ 扩展)...
正在读入 makefile “redis_mk.dep” (搜索路径) (没有 ~ 扩展)...
正在更新 makefile....
 正在考虑目标文件“redis_mk.dep”。
  正在为“redis_mk.dep”寻找隐含规则。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.o”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.c”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.cc”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.C”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.cpp”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.p”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.f”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.F”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.r”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.s”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.S”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.mod”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.sh”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep,v”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“RCS/redis_mk.dep,v”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“RCS/redis_mk.dep”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“s.redis_mk.dep”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“SCCS/s.redis_mk.dep”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.o”。
  正在寻找使用中间文件“redis_mk.dep.o”的规则。
   避免隐含规则递归。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.c”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.cc”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.C”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.cpp”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.p”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.f”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.F”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.r”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.s”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.S”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.mod”。
   以主干“redis_mk.dep.o”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.o,v”。
   以主干“redis_mk.dep.o”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk.dep.o,v”。
   以主干“redis_mk.dep.o”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk.dep.o”。
   以主干“redis_mk.dep.o”尝试匹配模式规则。
   尝试隐含前提“s.redis_mk.dep.o”。
   以主干“redis_mk.dep.o”尝试匹配模式规则。
   尝试隐含前提“SCCS/s.redis_mk.dep.o”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.c”。
   正在寻找使用中间文件“redis_mk.dep.c”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.y”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.l”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.w”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.w”。
    以主干“redis_mk.dep.c”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.c,v”。
    以主干“redis_mk.dep.c”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.c,v”。
    以主干“redis_mk.dep.c”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.c”。
    以主干“redis_mk.dep.c”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.dep.c”。
    以主干“redis_mk.dep.c”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.dep.c”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.y”。
    正在寻找使用中间文件“redis_mk.dep.y”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.dep.y”尝试匹配模式规则。
     尝试隐含前提“redis_mk.dep.y,v”。
     以主干“redis_mk.dep.y”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.y,v”。
     以主干“redis_mk.dep.y”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.y”。
     以主干“redis_mk.dep.y”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.dep.y”。
     以主干“redis_mk.dep.y”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.dep.y”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.l”。
    正在寻找使用中间文件“redis_mk.dep.l”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.dep.l”尝试匹配模式规则。
     尝试隐含前提“redis_mk.dep.l,v”。
     以主干“redis_mk.dep.l”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.l,v”。
     以主干“redis_mk.dep.l”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.l”。
     以主干“redis_mk.dep.l”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.dep.l”。
     以主干“redis_mk.dep.l”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.dep.l”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.w”。
    正在寻找使用中间文件“redis_mk.dep.w”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.dep.w”尝试匹配模式规则。
     尝试隐含前提“redis_mk.dep.w,v”。
     以主干“redis_mk.dep.w”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.w,v”。
     以主干“redis_mk.dep.w”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.w”。
     以主干“redis_mk.dep.w”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.dep.w”。
     以主干“redis_mk.dep.w”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.dep.w”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    拒绝不可能的隐含前提“redis_mk.dep.w”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.cc”。
   正在寻找使用中间文件“redis_mk.dep.cc”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.dep.cc”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.cc,v”。
    以主干“redis_mk.dep.cc”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.cc,v”。
    以主干“redis_mk.dep.cc”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.cc”。
    以主干“redis_mk.dep.cc”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.dep.cc”。
    以主干“redis_mk.dep.cc”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.dep.cc”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.C”。
   正在寻找使用中间文件“redis_mk.dep.C”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.dep.C”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.C,v”。
    以主干“redis_mk.dep.C”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.C,v”。
    以主干“redis_mk.dep.C”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.C”。
    以主干“redis_mk.dep.C”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.dep.C”。
    以主干“redis_mk.dep.C”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.dep.C”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.cpp”。
   正在寻找使用中间文件“redis_mk.dep.cpp”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.dep.cpp”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.cpp,v”。
    以主干“redis_mk.dep.cpp”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.cpp,v”。
    以主干“redis_mk.dep.cpp”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.cpp”。
    以主干“redis_mk.dep.cpp”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.dep.cpp”。
    以主干“redis_mk.dep.cpp”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.dep.cpp”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.p”。
   正在寻找使用中间文件“redis_mk.dep.p”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.web”。
    以主干“redis_mk.dep.p”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.p,v”。
    以主干“redis_mk.dep.p”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.p,v”。
    以主干“redis_mk.dep.p”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.p”。
    以主干“redis_mk.dep.p”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.dep.p”。
    以主干“redis_mk.dep.p”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.dep.p”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.web”。
    正在寻找使用中间文件“redis_mk.dep.web”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.dep.web”尝试匹配模式规则。
     尝试隐含前提“redis_mk.dep.web,v”。
     以主干“redis_mk.dep.web”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.web,v”。
     以主干“redis_mk.dep.web”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.web”。
     以主干“redis_mk.dep.web”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.dep.web”。
     以主干“redis_mk.dep.web”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.dep.web”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.f”。
   正在寻找使用中间文件“redis_mk.dep.f”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.F”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.r”。
    以主干“redis_mk.dep.f”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.f,v”。
    以主干“redis_mk.dep.f”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.f,v”。
    以主干“redis_mk.dep.f”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.f”。
    以主干“redis_mk.dep.f”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.dep.f”。
    以主干“redis_mk.dep.f”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.dep.f”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.F”。
    正在寻找使用中间文件“redis_mk.dep.F”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.dep.F”尝试匹配模式规则。
     尝试隐含前提“redis_mk.dep.F,v”。
     以主干“redis_mk.dep.F”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.F,v”。
     以主干“redis_mk.dep.F”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.F”。
     以主干“redis_mk.dep.F”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.dep.F”。
     以主干“redis_mk.dep.F”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.dep.F”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.r”。
    正在寻找使用中间文件“redis_mk.dep.r”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.dep”尝试匹配模式规则。
     拒绝不可能的隐含前提“redis_mk.dep.l”。
     以主干“redis_mk.dep.r”尝试匹配模式规则。
     尝试隐含前提“redis_mk.dep.r,v”。
     以主干“redis_mk.dep.r”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.r,v”。
     以主干“redis_mk.dep.r”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.r”。
     以主干“redis_mk.dep.r”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.dep.r”。
     以主干“redis_mk.dep.r”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.dep.r”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk.dep.F”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk.dep.r”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.s”。
   正在寻找使用中间文件“redis_mk.dep.s”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.S”。
    以主干“redis_mk.dep.s”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.s,v”。
    以主干“redis_mk.dep.s”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.s,v”。
    以主干“redis_mk.dep.s”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.s”。
    以主干“redis_mk.dep.s”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.dep.s”。
    以主干“redis_mk.dep.s”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.dep.s”。
    以主干“redis_mk.dep”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.S”。
    正在寻找使用中间文件“redis_mk.dep.S”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.dep.S”尝试匹配模式规则。
     尝试隐含前提“redis_mk.dep.S,v”。
     以主干“redis_mk.dep.S”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.S,v”。
     以主干“redis_mk.dep.S”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.dep.S”。
     以主干“redis_mk.dep.S”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.dep.S”。
     以主干“redis_mk.dep.S”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.dep.S”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk.dep.S”。
   以主干“redis_mk.dep”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.mod”。
   正在寻找使用中间文件“redis_mk.dep.mod”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.dep.mod”尝试匹配模式规则。
    尝试隐含前提“redis_mk.dep.mod,v”。
    以主干“redis_mk.dep.mod”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.mod,v”。
    以主干“redis_mk.dep.mod”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.dep.mod”。
    以主干“redis_mk.dep.mod”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.dep.mod”。
    以主干“redis_mk.dep.mod”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.dep.mod”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.c”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.cc”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.C”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.cpp”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.p”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.f”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.F”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.r”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.s”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.S”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.dep.mod”。
  以主干“redis_mk.dep”尝试匹配模式规则。
  尝试隐含前提“redis_mk.dep.sh”。
  正在寻找使用中间文件“redis_mk.dep.sh”的规则。
   避免隐含规则递归。
   以主干“redis_mk.dep.sh”尝试匹配模式规则。
   尝试隐含前提“redis_mk.dep.sh,v”。
   以主干“redis_mk.dep.sh”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk.dep.sh,v”。
   以主干“redis_mk.dep.sh”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk.dep.sh”。
   以主干“redis_mk.dep.sh”尝试匹配模式规则。
   尝试隐含前提“s.redis_mk.dep.sh”。
   以主干“redis_mk.dep.sh”尝试匹配模式规则。
   尝试隐含前提“SCCS/s.redis_mk.dep.sh”。
  找不到关于“redis_mk.dep”的隐含规则。
   正在考虑目标文件“dep”。
    文件“dep”不存在。
    目标文件“dep”的前提已完成。
   必须重新创建目标“dep”。
echo "src_dep"
将子进程 0x08c09090 (dep) PID 3980 放入链。
活跃子进程 0x08c09090 (dep) PID 3980 
src_dep
正在中止获胜的子进程 0x08c09090 PID 3980 
gcc -g3 -ggdb3 -rdynamic -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -MM *.c > redis_mk.dep
活跃子进程 0x08c09090 (dep) PID 3981 
正在中止获胜的子进程 0x08c09090 PID 3981 
Removing child 0x08c09090 PID 3981 from chain.
   重新创建目标文件“dep”成功。
  目标文件“redis_mk.dep”的前提已完成。
  目标“redis_mk.dep”的前提“dep”不存在。
 没有关于“redis_mk.dep”的命令，并且实际上改变了的前提。
 不需要重新创建目标“redis_mk.dep”.
 正在考虑目标文件“redis_mk-settings”。
  正在为“redis_mk-settings”寻找隐含规则。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.o”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.c”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.cc”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.C”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.cpp”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.p”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.f”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.F”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.r”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.s”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.S”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.mod”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.sh”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings,v”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“RCS/redis_mk-settings,v”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“RCS/redis_mk-settings”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“s.redis_mk-settings”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“SCCS/s.redis_mk-settings”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.o”。
  正在寻找使用中间文件“redis_mk-settings.o”的规则。
   避免隐含规则递归。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.c”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.cc”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.C”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.cpp”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.p”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.f”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.F”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.r”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.s”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.S”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.mod”。
   以主干“redis_mk-settings.o”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.o,v”。
   以主干“redis_mk-settings.o”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk-settings.o,v”。
   以主干“redis_mk-settings.o”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk-settings.o”。
   以主干“redis_mk-settings.o”尝试匹配模式规则。
   尝试隐含前提“s.redis_mk-settings.o”。
   以主干“redis_mk-settings.o”尝试匹配模式规则。
   尝试隐含前提“SCCS/s.redis_mk-settings.o”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.c”。
   正在寻找使用中间文件“redis_mk-settings.c”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.y”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.l”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.w”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.w”。
    以主干“redis_mk-settings.c”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.c,v”。
    以主干“redis_mk-settings.c”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.c,v”。
    以主干“redis_mk-settings.c”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.c”。
    以主干“redis_mk-settings.c”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk-settings.c”。
    以主干“redis_mk-settings.c”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk-settings.c”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.y”。
    正在寻找使用中间文件“redis_mk-settings.y”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk-settings.y”尝试匹配模式规则。
     尝试隐含前提“redis_mk-settings.y,v”。
     以主干“redis_mk-settings.y”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.y,v”。
     以主干“redis_mk-settings.y”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.y”。
     以主干“redis_mk-settings.y”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk-settings.y”。
     以主干“redis_mk-settings.y”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk-settings.y”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.l”。
    正在寻找使用中间文件“redis_mk-settings.l”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk-settings.l”尝试匹配模式规则。
     尝试隐含前提“redis_mk-settings.l,v”。
     以主干“redis_mk-settings.l”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.l,v”。
     以主干“redis_mk-settings.l”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.l”。
     以主干“redis_mk-settings.l”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk-settings.l”。
     以主干“redis_mk-settings.l”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk-settings.l”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.w”。
    正在寻找使用中间文件“redis_mk-settings.w”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk-settings.w”尝试匹配模式规则。
     尝试隐含前提“redis_mk-settings.w,v”。
     以主干“redis_mk-settings.w”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.w,v”。
     以主干“redis_mk-settings.w”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.w”。
     以主干“redis_mk-settings.w”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk-settings.w”。
     以主干“redis_mk-settings.w”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk-settings.w”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    拒绝不可能的隐含前提“redis_mk-settings.w”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.cc”。
   正在寻找使用中间文件“redis_mk-settings.cc”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk-settings.cc”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.cc,v”。
    以主干“redis_mk-settings.cc”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.cc,v”。
    以主干“redis_mk-settings.cc”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.cc”。
    以主干“redis_mk-settings.cc”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk-settings.cc”。
    以主干“redis_mk-settings.cc”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk-settings.cc”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.C”。
   正在寻找使用中间文件“redis_mk-settings.C”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk-settings.C”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.C,v”。
    以主干“redis_mk-settings.C”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.C,v”。
    以主干“redis_mk-settings.C”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.C”。
    以主干“redis_mk-settings.C”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk-settings.C”。
    以主干“redis_mk-settings.C”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk-settings.C”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.cpp”。
   正在寻找使用中间文件“redis_mk-settings.cpp”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk-settings.cpp”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.cpp,v”。
    以主干“redis_mk-settings.cpp”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.cpp,v”。
    以主干“redis_mk-settings.cpp”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.cpp”。
    以主干“redis_mk-settings.cpp”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk-settings.cpp”。
    以主干“redis_mk-settings.cpp”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk-settings.cpp”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.p”。
   正在寻找使用中间文件“redis_mk-settings.p”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.web”。
    以主干“redis_mk-settings.p”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.p,v”。
    以主干“redis_mk-settings.p”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.p,v”。
    以主干“redis_mk-settings.p”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.p”。
    以主干“redis_mk-settings.p”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk-settings.p”。
    以主干“redis_mk-settings.p”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk-settings.p”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.web”。
    正在寻找使用中间文件“redis_mk-settings.web”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk-settings.web”尝试匹配模式规则。
     尝试隐含前提“redis_mk-settings.web,v”。
     以主干“redis_mk-settings.web”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.web,v”。
     以主干“redis_mk-settings.web”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.web”。
     以主干“redis_mk-settings.web”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk-settings.web”。
     以主干“redis_mk-settings.web”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk-settings.web”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.f”。
   正在寻找使用中间文件“redis_mk-settings.f”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.F”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.r”。
    以主干“redis_mk-settings.f”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.f,v”。
    以主干“redis_mk-settings.f”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.f,v”。
    以主干“redis_mk-settings.f”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.f”。
    以主干“redis_mk-settings.f”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk-settings.f”。
    以主干“redis_mk-settings.f”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk-settings.f”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.F”。
    正在寻找使用中间文件“redis_mk-settings.F”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk-settings.F”尝试匹配模式规则。
     尝试隐含前提“redis_mk-settings.F,v”。
     以主干“redis_mk-settings.F”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.F,v”。
     以主干“redis_mk-settings.F”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.F”。
     以主干“redis_mk-settings.F”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk-settings.F”。
     以主干“redis_mk-settings.F”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk-settings.F”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.r”。
    正在寻找使用中间文件“redis_mk-settings.r”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk-settings”尝试匹配模式规则。
     拒绝不可能的隐含前提“redis_mk-settings.l”。
     以主干“redis_mk-settings.r”尝试匹配模式规则。
     尝试隐含前提“redis_mk-settings.r,v”。
     以主干“redis_mk-settings.r”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.r,v”。
     以主干“redis_mk-settings.r”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.r”。
     以主干“redis_mk-settings.r”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk-settings.r”。
     以主干“redis_mk-settings.r”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk-settings.r”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk-settings.F”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk-settings.r”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.s”。
   正在寻找使用中间文件“redis_mk-settings.s”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.S”。
    以主干“redis_mk-settings.s”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.s,v”。
    以主干“redis_mk-settings.s”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.s,v”。
    以主干“redis_mk-settings.s”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.s”。
    以主干“redis_mk-settings.s”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk-settings.s”。
    以主干“redis_mk-settings.s”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk-settings.s”。
    以主干“redis_mk-settings”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.S”。
    正在寻找使用中间文件“redis_mk-settings.S”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk-settings.S”尝试匹配模式规则。
     尝试隐含前提“redis_mk-settings.S,v”。
     以主干“redis_mk-settings.S”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.S,v”。
     以主干“redis_mk-settings.S”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk-settings.S”。
     以主干“redis_mk-settings.S”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk-settings.S”。
     以主干“redis_mk-settings.S”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk-settings.S”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk-settings.S”。
   以主干“redis_mk-settings”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.mod”。
   正在寻找使用中间文件“redis_mk-settings.mod”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk-settings.mod”尝试匹配模式规则。
    尝试隐含前提“redis_mk-settings.mod,v”。
    以主干“redis_mk-settings.mod”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.mod,v”。
    以主干“redis_mk-settings.mod”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk-settings.mod”。
    以主干“redis_mk-settings.mod”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk-settings.mod”。
    以主干“redis_mk-settings.mod”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk-settings.mod”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.c”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.cc”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.C”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.cpp”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.p”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.f”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.F”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.r”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.s”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.S”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk-settings.mod”。
  以主干“redis_mk-settings”尝试匹配模式规则。
  尝试隐含前提“redis_mk-settings.sh”。
  正在寻找使用中间文件“redis_mk-settings.sh”的规则。
   避免隐含规则递归。
   以主干“redis_mk-settings.sh”尝试匹配模式规则。
   尝试隐含前提“redis_mk-settings.sh,v”。
   以主干“redis_mk-settings.sh”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk-settings.sh,v”。
   以主干“redis_mk-settings.sh”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk-settings.sh”。
   以主干“redis_mk-settings.sh”尝试匹配模式规则。
   尝试隐含前提“s.redis_mk-settings.sh”。
   以主干“redis_mk-settings.sh”尝试匹配模式规则。
   尝试隐含前提“SCCS/s.redis_mk-settings.sh”。
  找不到关于“redis_mk-settings”的隐含规则。
   正在考虑目标文件“persist-settings”。
    文件“persist-settings”不存在。
     正在考虑目标文件“distclean”。
      文件“distclean”不存在。
       正在考虑目标文件“clean”。
        文件“clean”不存在。
        目标文件“clean”的前提已完成。
       必须重新创建目标“clean”。
echo "src_clean"
将子进程 0x08c0af58 (clean) PID 4036 放入链。
活跃子进程 0x08c0af58 (clean) PID 4036 
src_clean
正在中止获胜的子进程 0x08c0af58 PID 4036 
rm -rf redis-server26 redis-sentinel26 redis-cli26 redis-benchmark26 redis-check-dump26 redis-check-aof26 %.o
活跃子进程 0x08c0af58 (clean) PID 4037 
正在中止获胜的子进程 0x08c0af58 PID 4037 
Removing child 0x08c0af58 PID 4037 from chain.
       重新创建目标文件“clean”成功。
      目标文件“distclean”的前提已完成。
     必须重新创建目标“distclean”。
echo "src_distclean"
将子进程 0x08c09680 (distclean) PID 4038 放入链。
活跃子进程 0x08c09680 (distclean) PID 4038 
src_distclean
正在中止获胜的子进程 0x08c09680 PID 4038 
(cd ../deps && make -f redis_deps_mk distclean)
活跃子进程 0x08c09680 (distclean) PID 4039 
GNU Make 3.81
Copyright (C) 2006  Free Software Foundation, Inc.
该程序为自由软件，详情可参阅版权条款。在法律允许的范围内
我们不作任何担保，这包含但不限于任何商业适售性以及针对特
定目的的适用性的担保。

 这个程序创建为 i686-pc-linux-gnu
正在读入 makefiles...
正在读入 makefile “redis_deps_mk”...
make[1]: 正在进入目录 `/home/linux2014/WJT_2016/redis_test/redis_26/deps'
make[1]:正在离开目录 `/home/linux2014/WJT_2016/redis_test/redis_26/deps'
正在中止失败的子进程 0x08c09680 PID 4039 
(rm -f .make-*)
活跃子进程 0x08c09680 (distclean) PID 4041 
正在中止获胜的子进程 0x08c09680 PID 4041 
Removing child 0x08c09680 PID 4041 from chain.
     重新创建目标文件“distclean”成功。
    目标文件“persist-settings”的前提已完成。
   必须重新创建目标“persist-settings”。
echo STD= >> redis_mk-setttings
将子进程 0x08c09680 (persist-settings) PID 4043 放入链。
活跃子进程 0x08c09680 (persist-settings) PID 4043 
正在中止获胜的子进程 0x08c09680 PID 4043 
echo STD= >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4044 
正在中止获胜的子进程 0x08c09680 PID 4044 
echo WARN= >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4045 
正在中止获胜的子进程 0x08c09680 PID 4045 
echo OPT= >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4046 
正在中止获胜的子进程 0x08c09680 PID 4046 
echo MALLOC= >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4047 
正在中止获胜的子进程 0x08c09680 PID 4047 
echo CFLAGS= >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4048 
正在中止获胜的子进程 0x08c09680 PID 4048 
echo LDFLAGS= >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4049 
正在中止获胜的子进程 0x08c09680 PID 4049 
echo REDIS_CFLAGS= >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4050 
正在中止获胜的子进程 0x08c09680 PID 4050 
echo REDIS_LDFLAGS= >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4051 
正在中止获胜的子进程 0x08c09680 PID 4051 
echo PREV_FINAL_CFLAGS=-g3 -ggdb3 -rdynamic -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4052 
正在中止获胜的子进程 0x08c09680 PID 4052 
echo PREV_FINAL_LDFLAGS=-g3 -ggdb3 -rdynamic >> redis_mk-settings
活跃子进程 0x08c09680 (persist-settings) PID 4053 
正在中止获胜的子进程 0x08c09680 PID 4053 
(cd ../deps && make )
活跃子进程 0x08c09680 (persist-settings) PID 4054 
GNU Make 3.81
Copyright (C) 2006  Free Software Foundation, Inc.
该程序为自由软件，详情可参阅版权条款。在法律允许的范围内
我们不作任何担保，这包含但不限于任何商业适售性以及针对特
定目的的适用性的担保。

 这个程序创建为 i686-pc-linux-gnu
正在读入 makefiles...
正在读入 makefile “Makefile”...
正在更新 makefile....
 正在考虑目标文件“Makefile”。
  正在为“Makefile”寻找隐含规则。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.o”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.c”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.cc”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.C”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.cpp”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.p”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.f”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.F”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.r”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.s”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.S”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.mod”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.sh”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile,v”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“RCS/Makefile,v”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“RCS/Makefile”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“s.Makefile”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“SCCS/s.Makefile”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.o”。
  正在寻找使用中间文件“Makefile.o”的规则。
   避免隐含规则递归。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.c”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.cc”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.C”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.cpp”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.p”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.f”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.F”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.r”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.s”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.S”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.mod”。
   以主干“Makefile.o”尝试匹配模式规则。
   尝试隐含前提“Makefile.o,v”。
   以主干“Makefile.o”尝试匹配模式规则。
   尝试隐含前提“RCS/Makefile.o,v”。
   以主干“Makefile.o”尝试匹配模式规则。
   尝试隐含前提“RCS/Makefile.o”。
   以主干“Makefile.o”尝试匹配模式规则。
   尝试隐含前提“s.Makefile.o”。
   以主干“Makefile.o”尝试匹配模式规则。
   尝试隐含前提“SCCS/s.Makefile.o”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.c”。
   正在寻找使用中间文件“Makefile.c”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.y”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.l”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.w”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.w”。
    以主干“Makefile.c”尝试匹配模式规则。
    尝试隐含前提“Makefile.c,v”。
    以主干“Makefile.c”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.c,v”。
    以主干“Makefile.c”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.c”。
    以主干“Makefile.c”尝试匹配模式规则。
    尝试隐含前提“s.Makefile.c”。
    以主干“Makefile.c”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.Makefile.c”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.y”。
    正在寻找使用中间文件“Makefile.y”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“Makefile.y”尝试匹配模式规则。
     尝试隐含前提“Makefile.y,v”。
     以主干“Makefile.y”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.y,v”。
     以主干“Makefile.y”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.y”。
     以主干“Makefile.y”尝试匹配模式规则。
     尝试隐含前提“s.Makefile.y”。
     以主干“Makefile.y”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.Makefile.y”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.l”。
    正在寻找使用中间文件“Makefile.l”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“Makefile.l”尝试匹配模式规则。
     尝试隐含前提“Makefile.l,v”。
     以主干“Makefile.l”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.l,v”。
     以主干“Makefile.l”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.l”。
     以主干“Makefile.l”尝试匹配模式规则。
     尝试隐含前提“s.Makefile.l”。
     以主干“Makefile.l”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.Makefile.l”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.w”。
    正在寻找使用中间文件“Makefile.w”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“Makefile.w”尝试匹配模式规则。
     尝试隐含前提“Makefile.w,v”。
     以主干“Makefile.w”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.w,v”。
     以主干“Makefile.w”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.w”。
     以主干“Makefile.w”尝试匹配模式规则。
     尝试隐含前提“s.Makefile.w”。
     以主干“Makefile.w”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.Makefile.w”。
    以主干“Makefile”尝试匹配模式规则。
    拒绝不可能的隐含前提“Makefile.w”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.cc”。
   正在寻找使用中间文件“Makefile.cc”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“Makefile.cc”尝试匹配模式规则。
    尝试隐含前提“Makefile.cc,v”。
    以主干“Makefile.cc”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.cc,v”。
    以主干“Makefile.cc”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.cc”。
    以主干“Makefile.cc”尝试匹配模式规则。
    尝试隐含前提“s.Makefile.cc”。
    以主干“Makefile.cc”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.Makefile.cc”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.C”。
   正在寻找使用中间文件“Makefile.C”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“Makefile.C”尝试匹配模式规则。
    尝试隐含前提“Makefile.C,v”。
    以主干“Makefile.C”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.C,v”。
    以主干“Makefile.C”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.C”。
    以主干“Makefile.C”尝试匹配模式规则。
    尝试隐含前提“s.Makefile.C”。
    以主干“Makefile.C”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.Makefile.C”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.cpp”。
   正在寻找使用中间文件“Makefile.cpp”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“Makefile.cpp”尝试匹配模式规则。
    尝试隐含前提“Makefile.cpp,v”。
    以主干“Makefile.cpp”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.cpp,v”。
    以主干“Makefile.cpp”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.cpp”。
    以主干“Makefile.cpp”尝试匹配模式规则。
    尝试隐含前提“s.Makefile.cpp”。
    以主干“Makefile.cpp”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.Makefile.cpp”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.p”。
   正在寻找使用中间文件“Makefile.p”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.web”。
    以主干“Makefile.p”尝试匹配模式规则。
    尝试隐含前提“Makefile.p,v”。
    以主干“Makefile.p”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.p,v”。
    以主干“Makefile.p”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.p”。
    以主干“Makefile.p”尝试匹配模式规则。
    尝试隐含前提“s.Makefile.p”。
    以主干“Makefile.p”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.Makefile.p”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.web”。
    正在寻找使用中间文件“Makefile.web”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“Makefile.web”尝试匹配模式规则。
     尝试隐含前提“Makefile.web,v”。
     以主干“Makefile.web”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.web,v”。
     以主干“Makefile.web”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.web”。
     以主干“Makefile.web”尝试匹配模式规则。
     尝试隐含前提“s.Makefile.web”。
     以主干“Makefile.web”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.Makefile.web”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.f”。
   正在寻找使用中间文件“Makefile.f”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.F”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.r”。
    以主干“Makefile.f”尝试匹配模式规则。
    尝试隐含前提“Makefile.f,v”。
    以主干“Makefile.f”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.f,v”。
    以主干“Makefile.f”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.f”。
    以主干“Makefile.f”尝试匹配模式规则。
    尝试隐含前提“s.Makefile.f”。
    以主干“Makefile.f”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.Makefile.f”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.F”。
    正在寻找使用中间文件“Makefile.F”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“Makefile.F”尝试匹配模式规则。
     尝试隐含前提“Makefile.F,v”。
     以主干“Makefile.F”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.F,v”。
     以主干“Makefile.F”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.F”。
     以主干“Makefile.F”尝试匹配模式规则。
     尝试隐含前提“s.Makefile.F”。
     以主干“Makefile.F”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.Makefile.F”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.r”。
    正在寻找使用中间文件“Makefile.r”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“Makefile”尝试匹配模式规则。
     拒绝不可能的隐含前提“Makefile.l”。
     以主干“Makefile.r”尝试匹配模式规则。
     尝试隐含前提“Makefile.r,v”。
     以主干“Makefile.r”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.r,v”。
     以主干“Makefile.r”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.r”。
     以主干“Makefile.r”尝试匹配模式规则。
     尝试隐含前提“s.Makefile.r”。
     以主干“Makefile.r”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.Makefile.r”。
   以主干“Makefile”尝试匹配模式规则。
   拒绝不可能的隐含前提“Makefile.F”。
   以主干“Makefile”尝试匹配模式规则。
   拒绝不可能的隐含前提“Makefile.r”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.s”。
   正在寻找使用中间文件“Makefile.s”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.S”。
    以主干“Makefile.s”尝试匹配模式规则。
    尝试隐含前提“Makefile.s,v”。
    以主干“Makefile.s”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.s,v”。
    以主干“Makefile.s”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.s”。
    以主干“Makefile.s”尝试匹配模式规则。
    尝试隐含前提“s.Makefile.s”。
    以主干“Makefile.s”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.Makefile.s”。
    以主干“Makefile”尝试匹配模式规则。
    尝试隐含前提“Makefile.S”。
    正在寻找使用中间文件“Makefile.S”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“Makefile.S”尝试匹配模式规则。
     尝试隐含前提“Makefile.S,v”。
     以主干“Makefile.S”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.S,v”。
     以主干“Makefile.S”尝试匹配模式规则。
     尝试隐含前提“RCS/Makefile.S”。
     以主干“Makefile.S”尝试匹配模式规则。
     尝试隐含前提“s.Makefile.S”。
     以主干“Makefile.S”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.Makefile.S”。
   以主干“Makefile”尝试匹配模式规则。
   拒绝不可能的隐含前提“Makefile.S”。
   以主干“Makefile”尝试匹配模式规则。
   尝试隐含前提“Makefile.mod”。
   正在寻找使用中间文件“Makefile.mod”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“Makefile.mod”尝试匹配模式规则。
    尝试隐含前提“Makefile.mod,v”。
    以主干“Makefile.mod”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.mod,v”。
    以主干“Makefile.mod”尝试匹配模式规则。
    尝试隐含前提“RCS/Makefile.mod”。
    以主干“Makefile.mod”尝试匹配模式规则。
    尝试隐含前提“s.Makefile.mod”。
    以主干“Makefile.mod”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.Makefile.mod”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.c”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.cc”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.C”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.cpp”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.p”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.f”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.F”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.r”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.s”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.S”。
  以主干“Makefile”尝试匹配模式规则。
  拒绝不可能的隐含前提“Makefile.mod”。
  以主干“Makefile”尝试匹配模式规则。
  尝试隐含前提“Makefile.sh”。
  正在寻找使用中间文件“Makefile.sh”的规则。
   避免隐含规则递归。
   以主干“Makefile.sh”尝试匹配模式规则。
   尝试隐含前提“Makefile.sh,v”。
   以主干“Makefile.sh”尝试匹配模式规则。
   尝试隐含前提“RCS/Makefile.sh,v”。
   以主干“Makefile.sh”尝试匹配模式规则。
   尝试隐含前提“RCS/Makefile.sh”。
   以主干“Makefile.sh”尝试匹配模式规则。
   尝试隐含前提“s.Makefile.sh”。
   以主干“Makefile.sh”尝试匹配模式规则。
   尝试隐含前提“SCCS/s.Makefile.sh”。
  找不到关于“Makefile”的隐含规则。
  目标文件“Makefile”的前提已完成。
 不需要重新创建目标“Makefile”.
更新目标....
正在考虑目标文件“default”。
 文件“default”不存在。
 目标文件“default”的前提已完成。
必须重新创建目标“default”。
make[1]: 正在进入目录 `/home/linux2014/WJT_2016/redis_test/redis_26/deps'
将子进程 0x0a0602e0 (default) PID 4062 放入链。
活跃子进程 0x0a0602e0 (default) PID 4062 
Explicit target required
正在中止获胜的子进程 0x0a0602e0 PID 4062 
Removing child 0x0a0602e0 PID 4062 from chain.
重新创建目标文件“default”成功。
make[1]:正在离开目录 `/home/linux2014/WJT_2016/redis_test/redis_26/deps'
正在中止获胜的子进程 0x08c09680 PID 4054 
Removing child 0x08c09680 PID 4054 from chain.
   重新创建目标文件“persist-settings”成功。
  目标文件“redis_mk-settings”的前提已完成。
  目标“redis_mk-settings”的前提“persist-settings”不存在。
 没有关于“redis_mk-settings”的命令，并且实际上改变了的前提。
 不需要重新创建目标“redis_mk-settings”.
 正在考虑目标文件“redis_mk”。
  正在为“redis_mk”寻找隐含规则。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.o”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.c”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.cc”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.C”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.cpp”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.p”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.f”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.F”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.r”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.s”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.S”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.mod”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.sh”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk,v”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“RCS/redis_mk,v”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“RCS/redis_mk”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“s.redis_mk”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“SCCS/s.redis_mk”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.o”。
  正在寻找使用中间文件“redis_mk.o”的规则。
   避免隐含规则递归。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.c”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.cc”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.C”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.cpp”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.p”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.f”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.F”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.r”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.s”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.S”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.mod”。
   以主干“redis_mk.o”尝试匹配模式规则。
   尝试隐含前提“redis_mk.o,v”。
   以主干“redis_mk.o”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk.o,v”。
   以主干“redis_mk.o”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk.o”。
   以主干“redis_mk.o”尝试匹配模式规则。
   尝试隐含前提“s.redis_mk.o”。
   以主干“redis_mk.o”尝试匹配模式规则。
   尝试隐含前提“SCCS/s.redis_mk.o”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.c”。
   正在寻找使用中间文件“redis_mk.c”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.y”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.l”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.w”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.w”。
    以主干“redis_mk.c”尝试匹配模式规则。
    尝试隐含前提“redis_mk.c,v”。
    以主干“redis_mk.c”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.c,v”。
    以主干“redis_mk.c”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.c”。
    以主干“redis_mk.c”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.c”。
    以主干“redis_mk.c”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.c”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.y”。
    正在寻找使用中间文件“redis_mk.y”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.y”尝试匹配模式规则。
     尝试隐含前提“redis_mk.y,v”。
     以主干“redis_mk.y”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.y,v”。
     以主干“redis_mk.y”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.y”。
     以主干“redis_mk.y”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.y”。
     以主干“redis_mk.y”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.y”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.l”。
    正在寻找使用中间文件“redis_mk.l”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.l”尝试匹配模式规则。
     尝试隐含前提“redis_mk.l,v”。
     以主干“redis_mk.l”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.l,v”。
     以主干“redis_mk.l”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.l”。
     以主干“redis_mk.l”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.l”。
     以主干“redis_mk.l”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.l”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.w”。
    正在寻找使用中间文件“redis_mk.w”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.w”尝试匹配模式规则。
     尝试隐含前提“redis_mk.w,v”。
     以主干“redis_mk.w”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.w,v”。
     以主干“redis_mk.w”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.w”。
     以主干“redis_mk.w”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.w”。
     以主干“redis_mk.w”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.w”。
    以主干“redis_mk”尝试匹配模式规则。
    拒绝不可能的隐含前提“redis_mk.w”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.cc”。
   正在寻找使用中间文件“redis_mk.cc”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.cc”尝试匹配模式规则。
    尝试隐含前提“redis_mk.cc,v”。
    以主干“redis_mk.cc”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.cc,v”。
    以主干“redis_mk.cc”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.cc”。
    以主干“redis_mk.cc”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.cc”。
    以主干“redis_mk.cc”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.cc”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.C”。
   正在寻找使用中间文件“redis_mk.C”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.C”尝试匹配模式规则。
    尝试隐含前提“redis_mk.C,v”。
    以主干“redis_mk.C”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.C,v”。
    以主干“redis_mk.C”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.C”。
    以主干“redis_mk.C”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.C”。
    以主干“redis_mk.C”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.C”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.cpp”。
   正在寻找使用中间文件“redis_mk.cpp”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.cpp”尝试匹配模式规则。
    尝试隐含前提“redis_mk.cpp,v”。
    以主干“redis_mk.cpp”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.cpp,v”。
    以主干“redis_mk.cpp”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.cpp”。
    以主干“redis_mk.cpp”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.cpp”。
    以主干“redis_mk.cpp”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.cpp”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.p”。
   正在寻找使用中间文件“redis_mk.p”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.web”。
    以主干“redis_mk.p”尝试匹配模式规则。
    尝试隐含前提“redis_mk.p,v”。
    以主干“redis_mk.p”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.p,v”。
    以主干“redis_mk.p”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.p”。
    以主干“redis_mk.p”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.p”。
    以主干“redis_mk.p”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.p”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.web”。
    正在寻找使用中间文件“redis_mk.web”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.web”尝试匹配模式规则。
     尝试隐含前提“redis_mk.web,v”。
     以主干“redis_mk.web”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.web,v”。
     以主干“redis_mk.web”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.web”。
     以主干“redis_mk.web”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.web”。
     以主干“redis_mk.web”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.web”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.f”。
   正在寻找使用中间文件“redis_mk.f”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.F”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.r”。
    以主干“redis_mk.f”尝试匹配模式规则。
    尝试隐含前提“redis_mk.f,v”。
    以主干“redis_mk.f”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.f,v”。
    以主干“redis_mk.f”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.f”。
    以主干“redis_mk.f”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.f”。
    以主干“redis_mk.f”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.f”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.F”。
    正在寻找使用中间文件“redis_mk.F”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.F”尝试匹配模式规则。
     尝试隐含前提“redis_mk.F,v”。
     以主干“redis_mk.F”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.F,v”。
     以主干“redis_mk.F”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.F”。
     以主干“redis_mk.F”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.F”。
     以主干“redis_mk.F”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.F”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.r”。
    正在寻找使用中间文件“redis_mk.r”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk”尝试匹配模式规则。
     拒绝不可能的隐含前提“redis_mk.l”。
     以主干“redis_mk.r”尝试匹配模式规则。
     尝试隐含前提“redis_mk.r,v”。
     以主干“redis_mk.r”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.r,v”。
     以主干“redis_mk.r”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.r”。
     以主干“redis_mk.r”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.r”。
     以主干“redis_mk.r”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.r”。
   以主干“redis_mk”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk.F”。
   以主干“redis_mk”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk.r”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.s”。
   正在寻找使用中间文件“redis_mk.s”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.S”。
    以主干“redis_mk.s”尝试匹配模式规则。
    尝试隐含前提“redis_mk.s,v”。
    以主干“redis_mk.s”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.s,v”。
    以主干“redis_mk.s”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.s”。
    以主干“redis_mk.s”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.s”。
    以主干“redis_mk.s”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.s”。
    以主干“redis_mk”尝试匹配模式规则。
    尝试隐含前提“redis_mk.S”。
    正在寻找使用中间文件“redis_mk.S”的规则。
     避免隐含规则递归。
     避免隐含规则递归。
     避免隐含规则递归。
     以主干“redis_mk.S”尝试匹配模式规则。
     尝试隐含前提“redis_mk.S,v”。
     以主干“redis_mk.S”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.S,v”。
     以主干“redis_mk.S”尝试匹配模式规则。
     尝试隐含前提“RCS/redis_mk.S”。
     以主干“redis_mk.S”尝试匹配模式规则。
     尝试隐含前提“s.redis_mk.S”。
     以主干“redis_mk.S”尝试匹配模式规则。
     尝试隐含前提“SCCS/s.redis_mk.S”。
   以主干“redis_mk”尝试匹配模式规则。
   拒绝不可能的隐含前提“redis_mk.S”。
   以主干“redis_mk”尝试匹配模式规则。
   尝试隐含前提“redis_mk.mod”。
   正在寻找使用中间文件“redis_mk.mod”的规则。
    避免隐含规则递归。
    避免隐含规则递归。
    以主干“redis_mk.mod”尝试匹配模式规则。
    尝试隐含前提“redis_mk.mod,v”。
    以主干“redis_mk.mod”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.mod,v”。
    以主干“redis_mk.mod”尝试匹配模式规则。
    尝试隐含前提“RCS/redis_mk.mod”。
    以主干“redis_mk.mod”尝试匹配模式规则。
    尝试隐含前提“s.redis_mk.mod”。
    以主干“redis_mk.mod”尝试匹配模式规则。
    尝试隐含前提“SCCS/s.redis_mk.mod”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.c”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.cc”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.C”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.cpp”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.p”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.f”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.F”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.r”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.s”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.S”。
  以主干“redis_mk”尝试匹配模式规则。
  拒绝不可能的隐含前提“redis_mk.mod”。
  以主干“redis_mk”尝试匹配模式规则。
  尝试隐含前提“redis_mk.sh”。
  正在寻找使用中间文件“redis_mk.sh”的规则。
   避免隐含规则递归。
   以主干“redis_mk.sh”尝试匹配模式规则。
   尝试隐含前提“redis_mk.sh,v”。
   以主干“redis_mk.sh”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk.sh,v”。
   以主干“redis_mk.sh”尝试匹配模式规则。
   尝试隐含前提“RCS/redis_mk.sh”。
   以主干“redis_mk.sh”尝试匹配模式规则。
   尝试隐含前提“s.redis_mk.sh”。
   以主干“redis_mk.sh”尝试匹配模式规则。
   尝试隐含前提“SCCS/s.redis_mk.sh”。
  找不到关于“redis_mk”的隐含规则。
  目标文件“redis_mk”的前提已完成。
 不需要重新创建目标“redis_mk”.
更新目标....
正在考虑目标文件“install”。
 文件“install”不存在。
  正在考虑目标文件“all”。
   文件“all”不存在。
    正在考虑目标文件“redis-server26”。
     文件“redis-server26”不存在。
      正在考虑目标文件“adlist.o”。
       正在为“adlist.o”寻找隐含规则。
       以主干“adlist”尝试匹配模式规则。
       尝试隐含前提“adlist.c”。
       找到一条关于“adlist.o”的隐含规则。
        正在考虑目标文件“adlist.c”。
         正在为“adlist.c”寻找隐含规则。
         以主干“adlist”尝试匹配模式规则。
         尝试隐含前提“adlist.y”。
         以主干“adlist”尝试匹配模式规则。
         尝试隐含前提“adlist.l”。
         以主干“adlist”尝试匹配模式规则。
         尝试隐含前提“adlist.w”。
         以主干“adlist”尝试匹配模式规则。
         尝试隐含前提“adlist.w”。
         以主干“adlist.c”尝试匹配模式规则。
         尝试隐含前提“adlist.c,v”。
         以主干“adlist.c”尝试匹配模式规则。
         尝试隐含前提“RCS/adlist.c,v”。
         以主干“adlist.c”尝试匹配模式规则。
         尝试隐含前提“RCS/adlist.c”。
         以主干“adlist.c”尝试匹配模式规则。
         尝试隐含前提“s.adlist.c”。
         以主干“adlist.c”尝试匹配模式规则。
         尝试隐含前提“SCCS/s.adlist.c”。
         以主干“adlist”尝试匹配模式规则。
         尝试隐含前提“adlist.y”。
         正在寻找使用中间文件“adlist.y”的规则。
          避免隐含规则递归。
          以主干“adlist.y”尝试匹配模式规则。
          尝试隐含前提“adlist.y,v”。
          以主干“adlist.y”尝试匹配模式规则。
          尝试隐含前提“RCS/adlist.y,v”。
          以主干“adlist.y”尝试匹配模式规则。
          尝试隐含前提“RCS/adlist.y”。
          以主干“adlist.y”尝试匹配模式规则。
          尝试隐含前提“s.adlist.y”。
          以主干“adlist.y”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.adlist.y”。
         以主干“adlist”尝试匹配模式规则。
         尝试隐含前提“adlist.l”。
         正在寻找使用中间文件“adlist.l”的规则。
          避免隐含规则递归。
          以主干“adlist.l”尝试匹配模式规则。
          尝试隐含前提“adlist.l,v”。
          以主干“adlist.l”尝试匹配模式规则。
          尝试隐含前提“RCS/adlist.l,v”。
          以主干“adlist.l”尝试匹配模式规则。
          尝试隐含前提“RCS/adlist.l”。
          以主干“adlist.l”尝试匹配模式规则。
          尝试隐含前提“s.adlist.l”。
          以主干“adlist.l”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.adlist.l”。
         以主干“adlist”尝试匹配模式规则。
         尝试隐含前提“adlist.w”。
         正在寻找使用中间文件“adlist.w”的规则。
          避免隐含规则递归。
          以主干“adlist.w”尝试匹配模式规则。
          尝试隐含前提“adlist.w,v”。
          以主干“adlist.w”尝试匹配模式规则。
          尝试隐含前提“RCS/adlist.w,v”。
          以主干“adlist.w”尝试匹配模式规则。
          尝试隐含前提“RCS/adlist.w”。
          以主干“adlist.w”尝试匹配模式规则。
          尝试隐含前提“s.adlist.w”。
          以主干“adlist.w”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.adlist.w”。
         以主干“adlist”尝试匹配模式规则。
         拒绝不可能的隐含前提“adlist.w”。
         找不到关于“adlist.c”的隐含规则。
         目标文件“adlist.c”的前提已完成。
        不需要重新创建目标“adlist.c”.
        正在删除文件“adlist.c”。
        正在考虑目标文件“adlist.h”。
         正在为“adlist.h”寻找隐含规则。
         以主干“adlist.h”尝试匹配模式规则。
         尝试隐含前提“adlist.h,v”。
         以主干“adlist.h”尝试匹配模式规则。
         尝试隐含前提“RCS/adlist.h,v”。
         以主干“adlist.h”尝试匹配模式规则。
         尝试隐含前提“RCS/adlist.h”。
         以主干“adlist.h”尝试匹配模式规则。
         尝试隐含前提“s.adlist.h”。
         以主干“adlist.h”尝试匹配模式规则。
         尝试隐含前提“SCCS/s.adlist.h”。
         找不到关于“adlist.h”的隐含规则。
         目标文件“adlist.h”的前提已完成。
        不需要重新创建目标“adlist.h”.
        正在考虑目标文件“zmalloc.h”。
         正在为“zmalloc.h”寻找隐含规则。
         以主干“zmalloc.h”尝试匹配模式规则。
         尝试隐含前提“zmalloc.h,v”。
         以主干“zmalloc.h”尝试匹配模式规则。
         尝试隐含前提“RCS/zmalloc.h,v”。
         以主干“zmalloc.h”尝试匹配模式规则。
         尝试隐含前提“RCS/zmalloc.h”。
         以主干“zmalloc.h”尝试匹配模式规则。
         尝试隐含前提“s.zmalloc.h”。
         以主干“zmalloc.h”尝试匹配模式规则。
         尝试隐含前提“SCCS/s.zmalloc.h”。
         找不到关于“zmalloc.h”的隐含规则。
         目标文件“zmalloc.h”的前提已完成。
        不需要重新创建目标“zmalloc.h”.
       目标文件“adlist.o”的前提已完成。
       前提“adlist.c”比目标“adlist.o”旧。
       前提“adlist.c”比目标“adlist.o”旧。
       前提“adlist.h”比目标“adlist.o”旧。
       前提“zmalloc.h”比目标“adlist.o”新。
      必须重新创建目标“adlist.o”。
echo "%.o:%.c"
将子进程 0x08c0afe8 (adlist.o) PID 4063 放入链。
活跃子进程 0x08c0afe8 (adlist.o) PID 4063 
%.o:%.c
正在中止获胜的子进程 0x08c0afe8 PID 4063 
gcc -g3 -ggdb3 -rdynamic -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -c adlist.c adlist.h zmalloc.h
活跃子进程 0x08c0afe8 (adlist.o) PID 4064 
正在中止获胜的子进程 0x08c0afe8 PID 4064 
Removing child 0x08c0afe8 PID 4064 from chain.
      重新创建目标文件“adlist.o”成功。
      正在考虑目标文件“ae.o”。
       正在为“ae.o”寻找隐含规则。
       以主干“ae”尝试匹配模式规则。
       尝试隐含前提“ae.c”。
       找到一条关于“ae.o”的隐含规则。
        正在考虑目标文件“ae.c”。
         正在为“ae.c”寻找隐含规则。
         以主干“ae”尝试匹配模式规则。
         尝试隐含前提“ae.y”。
         以主干“ae”尝试匹配模式规则。
         尝试隐含前提“ae.l”。
         以主干“ae”尝试匹配模式规则。
         尝试隐含前提“ae.w”。
         以主干“ae”尝试匹配模式规则。
         尝试隐含前提“ae.w”。
         以主干“ae.c”尝试匹配模式规则。
         尝试隐含前提“ae.c,v”。
         以主干“ae.c”尝试匹配模式规则。
         尝试隐含前提“RCS/ae.c,v”。
         以主干“ae.c”尝试匹配模式规则。
         尝试隐含前提“RCS/ae.c”。
         以主干“ae.c”尝试匹配模式规则。
         尝试隐含前提“s.ae.c”。
         以主干“ae.c”尝试匹配模式规则。
         尝试隐含前提“SCCS/s.ae.c”。
         以主干“ae”尝试匹配模式规则。
         尝试隐含前提“ae.y”。
         正在寻找使用中间文件“ae.y”的规则。
          避免隐含规则递归。
          以主干“ae.y”尝试匹配模式规则。
          尝试隐含前提“ae.y,v”。
          以主干“ae.y”尝试匹配模式规则。
          尝试隐含前提“RCS/ae.y,v”。
          以主干“ae.y”尝试匹配模式规则。
          尝试隐含前提“RCS/ae.y”。
          以主干“ae.y”尝试匹配模式规则。
          尝试隐含前提“s.ae.y”。
          以主干“ae.y”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.ae.y”。
         以主干“ae”尝试匹配模式规则。
         尝试隐含前提“ae.l”。
         正在寻找使用中间文件“ae.l”的规则。
          避免隐含规则递归。
          以主干“ae.l”尝试匹配模式规则。
          尝试隐含前提“ae.l,v”。
          以主干“ae.l”尝试匹配模式规则。
          尝试隐含前提“RCS/ae.l,v”。
          以主干“ae.l”尝试匹配模式规则。
          尝试隐含前提“RCS/ae.l”。
          以主干“ae.l”尝试匹配模式规则。
          尝试隐含前提“s.ae.l”。
          以主干“ae.l”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.ae.l”。
         以主干“ae”尝试匹配模式规则。
         尝试隐含前提“ae.w”。
         正在寻找使用中间文件“ae.w”的规则。
          避免隐含规则递归。
          以主干“ae.w”尝试匹配模式规则。
          尝试隐含前提“ae.w,v”。
          以主干“ae.w”尝试匹配模式规则。
          尝试隐含前提“RCS/ae.w,v”。
          以主干“ae.w”尝试匹配模式规则。
          尝试隐含前提“RCS/ae.w”。
          以主干“ae.w”尝试匹配模式规则。
          尝试隐含前提“s.ae.w”。
          以主干“ae.w”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.ae.w”。
         以主干“ae”尝试匹配模式规则。
         拒绝不可能的隐含前提“ae.w”。
         找不到关于“ae.c”的隐含规则。
         目标文件“ae.c”的前提已完成。
        不需要重新创建目标“ae.c”.
        正在删除文件“ae.c”。
        正在考虑目标文件“ae.h”。
         正在为“ae.h”寻找隐含规则。
         以主干“ae.h”尝试匹配模式规则。
         尝试隐含前提“ae.h,v”。
         以主干“ae.h”尝试匹配模式规则。
         尝试隐含前提“RCS/ae.h,v”。
         以主干“ae.h”尝试匹配模式规则。
         尝试隐含前提“RCS/ae.h”。
         以主干“ae.h”尝试匹配模式规则。
         尝试隐含前提“s.ae.h”。
         以主干“ae.h”尝试匹配模式规则。
         尝试隐含前提“SCCS/s.ae.h”。
         找不到关于“ae.h”的隐含规则。
         目标文件“ae.h”的前提已完成。
        不需要重新创建目标“ae.h”.
        正在删除文件“zmalloc.h”。
        正在考虑目标文件“config.h”。
         正在为“config.h”寻找隐含规则。
         以主干“config.h”尝试匹配模式规则。
         尝试隐含前提“config.h,v”。
         以主干“config.h”尝试匹配模式规则。
         尝试隐含前提“RCS/config.h,v”。
         以主干“config.h”尝试匹配模式规则。
         尝试隐含前提“RCS/config.h”。
         以主干“config.h”尝试匹配模式规则。
         尝试隐含前提“s.config.h”。
         以主干“config.h”尝试匹配模式规则。
         尝试隐含前提“SCCS/s.config.h”。
         找不到关于“config.h”的隐含规则。
         目标文件“config.h”的前提已完成。
        不需要重新创建目标“config.h”.
        正在考虑目标文件“ae_epoll.c”。
         正在为“ae_epoll.c”寻找隐含规则。
         以主干“ae_epoll”尝试匹配模式规则。
         尝试隐含前提“ae_epoll.y”。
         以主干“ae_epoll”尝试匹配模式规则。
         尝试隐含前提“ae_epoll.l”。
         以主干“ae_epoll”尝试匹配模式规则。
         尝试隐含前提“ae_epoll.w”。
         以主干“ae_epoll”尝试匹配模式规则。
         尝试隐含前提“ae_epoll.w”。
         以主干“ae_epoll.c”尝试匹配模式规则。
         尝试隐含前提“ae_epoll.c,v”。
         以主干“ae_epoll.c”尝试匹配模式规则。
         尝试隐含前提“RCS/ae_epoll.c,v”。
         以主干“ae_epoll.c”尝试匹配模式规则。
         尝试隐含前提“RCS/ae_epoll.c”。
         以主干“ae_epoll.c”尝试匹配模式规则。
         尝试隐含前提“s.ae_epoll.c”。
         以主干“ae_epoll.c”尝试匹配模式规则。
         尝试隐含前提“SCCS/s.ae_epoll.c”。
         以主干“ae_epoll”尝试匹配模式规则。
         尝试隐含前提“ae_epoll.y”。
         正在寻找使用中间文件“ae_epoll.y”的规则。
          避免隐含规则递归。
          以主干“ae_epoll.y”尝试匹配模式规则。
          尝试隐含前提“ae_epoll.y,v”。
          以主干“ae_epoll.y”尝试匹配模式规则。
          尝试隐含前提“RCS/ae_epoll.y,v”。
          以主干“ae_epoll.y”尝试匹配模式规则。
          尝试隐含前提“RCS/ae_epoll.y”。
          以主干“ae_epoll.y”尝试匹配模式规则。
          尝试隐含前提“s.ae_epoll.y”。
          以主干“ae_epoll.y”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.ae_epoll.y”。
         以主干“ae_epoll”尝试匹配模式规则。
         尝试隐含前提“ae_epoll.l”。
         正在寻找使用中间文件“ae_epoll.l”的规则。
          避免隐含规则递归。
          以主干“ae_epoll.l”尝试匹配模式规则。
          尝试隐含前提“ae_epoll.l,v”。
          以主干“ae_epoll.l”尝试匹配模式规则。
          尝试隐含前提“RCS/ae_epoll.l,v”。
          以主干“ae_epoll.l”尝试匹配模式规则。
          尝试隐含前提“RCS/ae_epoll.l”。
          以主干“ae_epoll.l”尝试匹配模式规则。
          尝试隐含前提“s.ae_epoll.l”。
          以主干“ae_epoll.l”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.ae_epoll.l”。
         以主干“ae_epoll”尝试匹配模式规则。
         尝试隐含前提“ae_epoll.w”。
         正在寻找使用中间文件“ae_epoll.w”的规则。
          避免隐含规则递归。
          以主干“ae_epoll.w”尝试匹配模式规则。
          尝试隐含前提“ae_epoll.w,v”。
          以主干“ae_epoll.w”尝试匹配模式规则。
          尝试隐含前提“RCS/ae_epoll.w,v”。
          以主干“ae_epoll.w”尝试匹配模式规则。
          尝试隐含前提“RCS/ae_epoll.w”。
          以主干“ae_epoll.w”尝试匹配模式规则。
          尝试隐含前提“s.ae_epoll.w”。
          以主干“ae_epoll.w”尝试匹配模式规则。
          尝试隐含前提“SCCS/s.ae_epoll.w”。
         以主干“ae_epoll”尝试匹配模式规则。
         拒绝不可能的隐含前提“ae_epoll.w”。
         找不到关于“ae_epoll.c”的隐含规则。
         目标文件“ae_epoll.c”的前提已完成。
        不需要重新创建目标“ae_epoll.c”.
       目标文件“ae.o”的前提已完成。
       前提“ae.c”比目标“ae.o”旧。
       前提“ae.c”比目标“ae.o”旧。
       前提“ae.h”比目标“ae.o”旧。
       前提“zmalloc.h”比目标“ae.o”新。
       前提“config.h”比目标“ae.o”旧。
       前提“ae_epoll.c”比目标“ae.o”旧。
      必须重新创建目标“ae.o”。
echo "%.o:%.c"
将子进程 0x08c09f30 (ae.o) PID 4069 放入链。
活跃子进程 0x08c09f30 (ae.o) PID 4069 
%.o:%.c
正在中止获胜的子进程 0x08c09f30 PID 4069 
gcc -g3 -ggdb3 -rdynamic -I../deps/hiredis -I../deps/linenoise -I../deps/lua/src -c ae.c ae.h zmalloc.h config.h ae_epoll.c
活跃子进程 0x08c09f30 (ae.o) PID 4070 
正在中止失败的子进程 0x08c09f30 PID 4070 
Removing child 0x08c09f30 PID 4070 from chain.
